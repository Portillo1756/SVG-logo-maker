const shapes = require('../lib/shapes.test.js');

// TODO: CREATED MY TEST
describe('shapes', () => {
 
describe('i expect when shape is call i will get a circle, triangle, or square', () => {
    it('should take a shape and get a circle, triangle or a square', () => {
        
        expect(shapes)
    })
});
});